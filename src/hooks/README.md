# hooks/

Кастомные React-хуки.

| Хук | Описание |
|---|---|
| `useDebounce<T>(value, delay)` | Возвращает дебаунсированное значение: обновляет его только спустя `delay` мс после последнего изменения `value`. Используется для откладывания поискового запроса на 500 мс. |
